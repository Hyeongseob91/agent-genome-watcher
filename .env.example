# Upstage API
UPSTAGE_API_KEY=your_api_key_here

# Mock mode
# Set to "true" to use mock data (no API calls, no real crawling)
# Set to "false" to use real Upstage API and Playwright crawling
MOCK_MODE=true
